class InputDataReceiver{
    #text;
    #textFile;
    #JsonFile;

    getText(text);
    getTextFile(textFile);
    getJsonFile(JsonFile); // ar putea merge un chain of responsibility si sa verific daca typeof input primit este de tipul celor din paranteza
}